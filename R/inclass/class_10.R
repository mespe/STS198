# May 3rd
# STS 198

load("../../data/distracted.Rda")

colnames(mtcars)

dim(mtcars)
rownames(mtcars)

names()

rownames(mtcars)[which(mtcars$cyl == 6)]

mtcars$mpg[which(mtcars$cyl == 6)]

colnames(mtcars)[1:3]

x = c(a = 1, b = 2)

subset()

x == 2

    
